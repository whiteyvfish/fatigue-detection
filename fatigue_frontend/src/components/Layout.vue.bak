<template>
  <div class="common-layout">
    <el-container>
      <el-aside width="200px" class="aside">
        <div class="logo">
          <el-icon size="24"><Monitor /></el-icon>
          <span>疲劳检测系统</span>
        </div>
        <el-menu
            :default-active="$route.path"
            router
            class="el-menu-vertical"
            background-color="#304156"
            text-color="#bfcbd9"
            active-text-color="#409EFF"
        >
          <el-menu-item v-for="route in routes" :key="route.path" :index="route.path">
            <el-icon>
              <component :is="route.meta.icon" />
            </el-icon>
            <span>{{ route.meta.title }}</span>
          </el-menu-item>
        </el-menu>
      </el-aside>

      <el-container>
        <el-header class="header">
          <div class="header-title">基于CBAM的疲劳驾驶检测系统</div>
          <div class="user-info">
            <el-tag type="success">在线</el-tag>
            <span style="margin-left: 10px;">用户: user001</span>
          </div>
        </el-header>

        <el-main class="main-content">
          <router-view />
        </el-main>
      </el-container>
    </el-container>
  </div>
</template>

<script setup>
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import { Monitor, Picture, VideoCamera, List, TrendCharts } from '@element-plus/icons-vue'

const route = useRoute()
const routes = computed(() => {
  return route.matched[0]?.children || []
})
</script>

<style scoped>
.common-layout {
  min-height: 100vh;
  background: linear-gradient(180deg, #f5f7fb 0%, #e8eef7 100%);
}

.aside {
  background: linear-gradient(180deg, #1f2d3d 0%, #283a52 100%);
  color: #e6eef7;
  box-shadow: 2px 0 20px rgba(15, 34, 57, 0.15);
}

.logo {
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  font-weight: 700;
  letter-spacing: 1px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.logo .el-icon {
  margin-right: 12px;
  color: #50bfff;
}

.header {
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(12px);
  box-shadow: 0 10px 30px rgba(23, 43, 77, 0.08);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 26px;
  min-height: 74px;
}

.header-title {
  font-size: 22px;
  font-weight: 700;
  color: #1b2a41;
}

.user-info {
  display: flex;
  align-items: center;
  color: #4c596d;
}

.user-info span {
  margin-left: 12px;
  font-weight: 500;
}

.main-content {
  background-color: #f8fbff;
  padding: 26px;
  border-radius: 18px;
  min-height: calc(100vh - 138px);
  box-shadow: inset 0 0 0 1px rgba(69, 113, 186, 0.04);
  overflow-y: auto;
}

.el-menu-vertical {
  border-right: none;
  margin-top: 16px;
}

.el-menu-item {
  border-radius: 14px;
  margin: 6px 12px;
  transition: all 0.25s ease;
}

.el-menu-item:hover {
  background: rgba(255, 255, 255, 0.14);
}

.el-menu-item.is-active {
  background: rgba(77, 144, 255, 0.18) !important;
}

.el-menu-item .el-icon {
  margin-right: 12px;
  color: #9fc9ff;
}

.el-menu-item span {
  color: #e6eef7;
}

.el-menu-item.is-active span {
  color: #ffffff;
}
</style>